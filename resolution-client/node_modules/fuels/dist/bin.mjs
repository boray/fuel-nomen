#!/usr/bin/env node
import{configureCliOptions as t}from"@fuel-ts/abi-typegen/cli";import{versions as m}from"@fuel-ts/versions";import{run as p}from"@fuel-ts/versions/cli";import{Command as c}from"commander";function n(r){let o=new c;o.name("fuels"),o.version(m.FUELS),o.command("versions").description("check for version incompatibilities").action(p);let i=o.command("typegen").description("generate typescript from contract abi json files");t(i),o.parse(r)}n(process.argv);
//# sourceMappingURL=bin.mjs.map