import { Command } from 'commander';
import { P as ProgramTypeEnum } from './ProgramTypeEnum-4274f664.js';

interface ICliParams {
    inputs: string[];
    output: string;
    silent: boolean;
    contract: boolean;
    script: boolean;
    predicate: boolean;
}
declare function resolveProgramType(params: {
    contract: boolean;
    script: boolean;
    predicate: boolean;
}): ProgramTypeEnum;
declare function runCliAction(options: ICliParams): void;
declare function configureCliOptions(program: Command): void;
declare function run(params: {
    argv: string[];
    programName: string;
}): void;

export { ICliParams, configureCliOptions, resolveProgramType, run, runCliAction };
